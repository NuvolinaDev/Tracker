//function addGame(title, platforms) {
//  const key = "games";
//const stored = loadItems(key);

//platforms.forEach((platform) => {
//if (
//!stored.some(
//(i) =>
// i.title.toLowerCase() === title.toLowerCase() &&
//i.platform === platform
//)
//) {
//stored.push({ title, platform });
//}
//});

//saveItems(key, stored);
//}
